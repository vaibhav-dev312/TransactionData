@using TransactionData.Common
@model TransactionData.Models.UploadTransactionModel


@using (Html.BeginForm("UploadTransactionData", "Transaction", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h2><b>Upload Transaction Data</b></h2>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })

        <div class="form-group">
            <div class="col-md-12">
                <h4>
                    @Html.TextBoxFor(model => model.UploadTransactionFile, new { type = "file" })
                </h4>
            </div>
        </div>
        <div class="col-md-12 form-group">
            @if (ViewBag.Result == Constants.UnknownFileErrorMsg || ViewBag.Result == Constants.SelectFileMsg || ViewBag.Result == Constants.FileSizeExccedErrorMsg)
            {
                <h4 class="text-left" style="color:red;">@ViewBag.Result </h4>
            }
            else
            {
                <h4 class="text-left" style="color:green">@ViewBag.Result </h4>
            }

        </div>
        <div class="form-group">
            <div class="col-md-10">
                <input type="submit" value="Upload" class="btn btn-primary" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index", "Home")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")

}

